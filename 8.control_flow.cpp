#include <iostream>
#include "Log.h"

// there are 3 main control flow statements -> continue, break, return
// continue says go to the next iteration of this loop if it exists
// return will exit the function
// return can be written anywhere not like continue and break that are called in a loop

int main()
{
    std::cin.get();
}

